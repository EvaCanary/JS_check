//massives

//обьявление массива
// const items = ['Sergey', 'Dmirty','Vasily'];

// //Чтение данных из массива
// console.log(items.length);

//Проход по элементам массива
// function printItems(items) {
// for (let index = 0; index < names.length; index++) {
//     const element = names[index];
    
//     console.log(name);
// }}

// function ptintItems(items) {
//     items.forEach(function(item, index) {
//         console.log(item, index);
//     });
// }




//обьявление массива

// const items  = ['светильник', 'юр адрес', 'попугай']

// const parrotIndex = getParrotIndex(items);

// console.log(items [parrotIndex]);

// function getParrotIndex(items) {
//     let parrotIndex = null;

//     items.forEach(function(item, index) {
//         if (item === 'попугай') {
//             parrotIndex = index;
//         }
//     });

//     return parrotIndex;
// }

//search massive

const numbers = [1,2,3,4,5];
const langs = ['HTML', 'JavaScript', 'Python', 'PHP'];
const basket = [ ];

const result = numbers.indexOf(3);
console.log('result', result)